# ContextCache (MVP)

A shared, opt-in project memory layer for teams using different AI tools.
Humans publish high-signal “memory cards”, and the system returns “memory packs”
to paste into ChatGPT/Claude/Ollama.
